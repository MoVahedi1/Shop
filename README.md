# فروشگاه آنلاین لوازم خانگی

این پروژه یک وب‌سایت نمونه برای فروشگاه آنلاین لوازم خانگی با نام "نمونه کار لوازم خانگی" است که به زبان فارسی و با پشتیبانی کامل RTL طراحی شده است.

## 📋 فهرست محتوا

- [ویژگی‌ها](#ویژگی‌ها)
- [ساختار پروژه](#ساختار-پروژه)
- [نحوه استفاده](#نحوه-استفاده)
- [سفارشی‌سازی](#سفارشی‌سازی)
  - [تغییر محصولات](#تغییر-محصولات-پیش‌فرض)
  - [تغییر رنگ‌ها](#تغییر-رنگ‌ها)
  - [تغییر فونت](#تغییر-فونت)
- [ساختار HTML](#ساختار-html)
- [نکات توسعه](#نکات-توسعه)

## ✨ ویژگی‌ها

- ✅ طراحی کاملاً ریسپانسیو (موبایل، تبلت، دسکتاپ)
- ✅ پشتیبانی کامل RTL برای زبان فارسی
- ✅ دسترسی‌پذیری (Accessibility) با استفاده از ARIA attributes
- ✅ HTML معنایی (Semantic HTML)
- ✅ CSS مدرن با استفاده از CSS Variables
- ✅ بدون وابستگی به کتابخانه‌های خارجی (Vanilla JavaScript)
- ✅ بهینه‌سازی تصاویر با lazy loading
- ✅ اعتبارسنجی فرم‌ها با HTML5
- ✅ کامنت‌های فارسی در تمام بخش‌ها

## 📁 ساختار پروژه

```
shop/
├── index.html          # صفحه اصلی
├── products.html       # صفحه لیست محصولات
├── product.html        # صفحه جزئیات محصول
├── cart.html          # صفحه سبد خرید
├── checkout.html      # صفحه پرداخت
├── styles.css         # فایل استایل اصلی
└── README.md          # این فایل
```

## 🚀 نحوه استفاده

1. تمام فایل‌ها را در یک پوشه قرار دهید
2. فایل `index.html` را در مرورگر باز کنید
3. برای مشاهده صفحات دیگر از لینک‌های موجود در منو استفاده کنید

**نکته:** برای استفاده کامل از فونت‌های فارسی (Vazirmatn)، باید به اینترنت متصل باشید. در صورت عدم اتصال، از فونت سیستم استفاده می‌شود.

## 🎨 سفارشی‌سازی

### تغییر محصولات پیش‌فرض

محصولات در فایل‌های HTML به صورت hard-coded قرار گرفته‌اند. برای تغییر آن‌ها:

#### روش 1: تغییر مستقیم در HTML

در هر فایل HTML (مثلاً `index.html`، `products.html`)، بخش‌های مربوط به محصولات را پیدا کرده و تغییر دهید:

```html
<!-- نمونه کارت محصول -->
<article class="product-card">
  <img src="آدرس-تصویر-جدید.jpg" alt="نام محصول" class="product-image">
  <div class="product-info">
    <p class="product-brand">برند: نام برند</p>
    <h3 class="product-title">نام محصول</h3>
    <div class="product-price">
      <span class="price-current">قیمت جدید</span>
    </div>
    <!-- ... -->
  </div>
</article>
```

#### روش 2: استفاده از JavaScript و داده‌های JSON (پیشرفته)

می‌توانید داده‌های محصولات را در یک فایل JSON قرار داده و با JavaScript آن‌ها را در صفحه رندر کنید:

1. یک فایل `products.json` ایجاد کنید:
```json
[
  {
    "id": 1,
    "title": "نام محصول",
    "brand": "برند",
    "price": 13500000,
    "price_display": "۱۳,۵۰۰,۰۰۰ تومان",
    "old_price": 15000000,
    "image": "آدرس-تصویر.jpg",
    "rating": 4.6,
    "reviews": 128,
    "stock": 12,
    "tag": "پرفروش"
  }
]
```

2. با JavaScript محصولات را لود کنید:
```javascript
fetch('products.json')
  .then(response => response.json())
  .then(products => {
    // رندر محصولات در DOM
  });
```

### تغییر رنگ‌ها

تمام رنگ‌های سایت در فایل `styles.css` و در بخش `:root` به صورت CSS Variables تعریف شده‌اند:

```css
:root {
  /* رنگ‌های اصلی */
  --color-primary: #2563eb;          /* آبی اصلی */
  --color-primary-dark: #1e40af;      /* آبی تیره */
  --color-secondary: #059669;         /* سبز */
  --color-accent: #dc2626;            /* قرمز */
  
  /* رنگ‌های پس‌زمینه */
  --bg-primary: #ffffff;              /* سفید */
  --bg-secondary: #f9fafb;            /* خاکستری روشن */
  
  /* ... */
}
```

برای تغییر رنگ‌ها، فقط مقادیر این متغیرها را تغییر دهید. برای مثال:

```css
:root {
  --color-primary: #8b5cf6;  /* تغییر به رنگ بنفش */
  --color-accent: #f59e0b;  /* تغییر به رنگ نارنجی */
}
```

### تغییر فونت

#### استفاده از فونت‌های وب فارسی

در تگ `<head>` هر فایل HTML، لینک فونت را تغییر دهید:

```html
<!-- مثال: استفاده از فونت ایران‌سنس -->
<link href="https://fonts.googleapis.com/css2?family=IRANSans:wght@400;600;700&display=swap" rel="stylesheet">
<style>
  body { font-family: 'IRANSans', sans-serif; }
</style>
```

#### استفاده از فونت محلی

1. فونت را در پوشه `fonts/` قرار دهید
2. در `styles.css` فونت را تعریف کنید:

```css
@font-face {
  font-family: 'MyFont';
  src: url('fonts/MyFont.woff2') format('woff2');
}

:root {
  --font-family: 'MyFont', 'Vazirmatn', sans-serif;
}
```

#### تغییر فونت سیستم

برای استفاده از فونت سیستم:

```css
:root {
  --font-family: 'Tahoma', 'Arial', sans-serif;
}
```

## 📐 ساختار HTML

### صفحات موجود

1. **index.html**: صفحه اصلی با بخش‌های:
   - هدر و ناوبری
   - بخش Hero
   - دسته‌بندی‌های محصولات
   - محصولات پرفروش
   - نظرات مشتریان
   - فوتر

2. **products.html**: لیست محصولات با:
   - سایدبار فیلتر (دسته، قیمت، برند، موجودی)
   - کنترل‌های مرتب‌سازی
   - گرید محصولات
   - صفحه‌بندی

3. **product.html**: جزئیات محصول با:
   - گالری تصاویر
   - اطلاعات کامل محصول
   - تب‌های توضیحات، مشخصات فنی و نظرات

4. **cart.html**: سبد خرید با:
   - لیست آیتم‌ها
   - امکان تغییر تعداد و حذف
   - خلاصه سفارش
   - فرم اطلاعات مشتری

5. **checkout.html**: پرداخت با:
   - فرم اطلاعات شخصی
   - فرم آدرس تحویل
   - انتخاب روش پرداخت
   - انتخاب روش ارسال
   - اعتبارسنجی فرم

## 💡 نکات توسعه

### تبدیل به CMS یا Framework

این کد به راحتی قابل تبدیل به یک سیستم مدیریت محتوا یا فریم‌ورک است:

#### React/Next.js
- تبدیل کامپوننت‌های HTML به React Components
- استفاده از State Management برای سبد خرید
- API Routes برای محصولات

#### WordPress
- تبدیل به قالب WordPress
- استفاده از Custom Post Types برای محصولات
- استفاده از WooCommerce

#### Vue.js/Nuxt.js
- تبدیل به Vue Components
- استفاده از Vuex برای State Management

### افزودن Backend

برای افزودن قابلیت‌های Backend:

1. **API برای محصولات**: ایجاد REST API یا GraphQL
2. **سیستم احراز هویت**: برای کاربران
3. **پرداخت واقعی**: اتصال به درگاه‌های بانکی (زرین‌پال، سامان و...)
4. **سیستم مدیریت محتوا**: برای مدیریت محصولات و سفارش‌ها

### بهینه‌سازی

- **تصاویر**: استفاده از فرمت WebP و تصاویر بهینه‌شده
- **کش**: استفاده از Service Workers برای Offline Support
- **بارگذاری**: Lazy Loading برای تصاویر و کدهای JavaScript
- **Minify**: فشرده‌سازی CSS و JavaScript برای Production

## 📝 داده‌های پیش‌فرض محصولات

محصولات پیش‌فرض که در سایت استفاده شده:

1. یخچال فریزر مدل A100 - برند سامانه - ۱۳,۵۰۰,۰۰۰ تومان
2. ماشین لباس‌شویی 7 کیلو مدل W70 - برند بهمن - ۴,۲۰۰,۰۰۰ تومان
3. تلویزیون هوشمند 50 اینچ - برند نوا - ۸,۸۰۰,۰۰۰ تومان
4. جاروبرقی بی‌سیم مدل V20 - برند پاکان - ۱,۸۵۰,۰۰۰ تومان
5. مایکروویو دیجیتال 25 لیتر - برند آوا - ۹۵۰,۰۰۰ تومان (ناموجود)
6. آبمیوه‌گیری چندکاره - برند سفره - ۴۵۰,۰۰۰ تومان

برای تغییر این محصولات، بخش "تغییر محصولات پیش‌فرض" را مطالعه کنید.

## 🔧 مشکلات رایج

### تصاویر نمایش داده نمی‌شوند
- بررسی کنید که لینک‌های placeholder صحیح هستند
- در صورت استفاده از تصاویر محلی، مسیر آن‌ها را بررسی کنید

### فونت فارسی نمایش داده نمی‌شود
- بررسی کنید که به اینترنت متصل هستید (برای فونت‌های Google Fonts)
- یا فونت را به صورت محلی نصب کنید

### ریسپانسیو نیست
- بررسی کنید که viewport meta tag در `<head>` وجود دارد:
  ```html
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  ```

## 📄 لایسنس

این پروژه یک نمونه کار است و برای استفاده شخصی و تجاری آزاد است.

## 🙏 تشکر

از استفاده از این قالب ممنونیم! برای سوالات و پیشنهادات می‌توانید Issues ایجاد کنید.

---

**نکته نهایی**: این پروژه به عنوان یک نمونه کار طراحی شده و برای استفاده در محیط Production نیاز به تغییرات و بهبودهایی دارد، به خصوص در بخش Backend و اتصال به API های واقعی.
